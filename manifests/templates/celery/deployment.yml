---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.service.celery.name }}
  namespace: {{ .Release.Namespace }}
  labels: &Labels
    app: {{ .Values.service.celery.name }}
spec:
  replicas: {{ .Values.service.celery.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 50%
  selector:
    matchLabels: *Labels
  template:
    metadata:
      labels: *Labels
    spec:
      containers:
      - name: {{ .Values.service.celery.name }}
        image: "{{ .Values.service.celery.image }}"
        imagePullPolicy: Always
        command: ["sh", "-c", "/celery_entrypoint.sh"]
        env:
          {{- range .Values.service.celery.secrets }}
          - name: {{ .env_variable }}
            valueFrom:
              secretKeyRef:
                name: {{ .secret_map }}
                key: {{ .secret_key }}
          {{- end }}
