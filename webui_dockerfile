FROM node:alpine3.14

ENV PATH /webui/node_modules/.bin:$PATH
COPY webui/package.json ./
COPY webui/package-lock.json ./
RUN npm install --silent && yarn add -D webpack-cli
COPY webui ./
WORKDIR /webui
EXPOSE 8080
CMD ["npm", "run", "build"]
